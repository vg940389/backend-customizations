apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "backend-customizations.fullname" . }}-test-connection"
  labels:
    {{- include "backend-customizations.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "backend-customizations.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
